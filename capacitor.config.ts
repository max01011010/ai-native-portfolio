import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.ainativedevfolio',
  appName: 'AI Native Dev Folio',
  webDir: 'dist'
};

export default config;
